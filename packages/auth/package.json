{
  "name": "@wishbeam/auth",
  "private": true,
  "type": "module",
  "imports": {
    "#*": "./src/*"
  },
  "exports": {
    ".": "./src/index.ts",
    "./permissions": "./src/permissions.ts"
  },
  "scripts": {
    "clean": "bun exec \"rm -rf node_modules\"",
    "format": "prettier . --ignore-path ../../.gitignore --ignore-path ../../.prettierignore --write",
    "format:check": "prettier . --ignore-path ../../.gitignore --ignore-path ../../.prettierignore --check",
    "generate-db-schema": "cross-env DATABASE_URL=url pnpm dlx @better-auth/cli@latest generate --config src/index.ts",
    "lint": "eslint",
    "lint:fix": "eslint --fix",
    "typecheck": "tsc --noEmit"
  },
  "prettier": "@wishbeam/prettier-config",
  "dependencies": {
    "@wishbeam/db": "workspace:*",
    "@wishbeam/env": "workspace:*",
    "better-auth": "catalog:"
  },
  "devDependencies": {
    "@types/bun": "catalog:",
    "@wishbeam/eslint-config": "workspace:*",
    "@wishbeam/typescript-config": "workspace:*",
    "typescript": "catalog:"
  }
}
